# CLAUDE.md — game-lab（カード / シミュレーションゲーム開発規約）

このファイルはセッションをまたいで保持される恒久的な前提条件である。
作業開始時に必ず全文を読み、以下の規約に反する実装を行わないこと。

---

## 1. 技術スタック（変更禁止）

| 項目 | 採用 |
|---|---|
| 言語 | TypeScript（strict: true） |
| ビルド | Vite |
| テスト | Vitest |
| 描画 | DOM + CSS（Canvasはエフェクト等の限定用途のみ） |
| 状態管理 | 自前のreducer。外部ライブラリ禁止 |
| 配布 | 静的ビルド。Android Chrome 縦持ちで動作すること |

外部ライブラリの追加は原則禁止。必要な場合は実装前に理由を提案し、承認を得ること。

---

## 2. ディレクトリ構成

```
games/<game-name>/
  SPEC.md          仕様書。実装より先に必ず作成する
  src/
    core/          純粋ロジック層（後述の制約を厳守）
    game/          描画・入力・音。core を呼ぶだけ
    data/          カード定義・パラメータ等のデータ
  tests/           core のみを対象とするテスト
  tools/           バランス調整用シミュレータ等のCLI
```

---

## 3. core 層の制約（最重要）

`src/core/` は **純粋関数のみ**で構成する。以下を core 内で使用してはならない。

- `Math.random()` / `Date.now()` / `new Date()` / `setTimeout` / `performance.now()`
- `window` / `document` / `localStorage` などブラウザAPI全般
- `console.log` 以外の副作用

乱数は必ずシード付きPRNGを状態として持ち回る。

```ts
// core の唯一の形
function reduce(state: GameState, action: Action): GameState
```

この制約により、core はエンジンなしでテスト可能かつ完全に決定論的になる。
**この制約を破る実装は、動作していても不合格とする。**

---

## 4. カード効果はデータで表現する

効果を関数やswitch文で直接書かず、必ずデータとして定義する。

```ts
// 良い例
{ id: "c001", name: "急行運転", cost: 2,
  effects: [{ type: "draw", value: 2 }, { type: "damage", target: "opponent", value: 1 }] }

// 悪い例
{ id: "c001", onPlay: (s) => { /* 直接処理 */ } }
```

効果タイプの解釈は `core/effects.ts` に一元化する。
カード追加時にロジックコードを触る必要がない状態を維持すること。

---

## 5. 検証（実装完了の定義）

以下がすべて通って初めて「完成」とみなす。納品前に必ず実行し、結果を報告すること。

```bash
npm run typecheck    # tsc --noEmit
npm run test         # vitest run
npm run sim          # tools/ のバランスシミュレーション
npm run build        # 本番ビルド成功
```

### 必須テスト
- **決定論リプレイ**: 同一シード＋同一入力列 → 最終状態ハッシュが完全一致
- **ルール不変条件**: 手札枚数・リソース・HP等が負値やNaNにならない
- **終局保証**: 1万回の自動対戦がすべて規定ターン以内に終了する（無限ループ検出）

### バランスシミュレーション（`npm run sim`）
1万回の自動対戦を実行し、以下を出力する。
- 先手勝率（45〜55%を逸脱したら要調整）
- 平均ターン数・平均試合時間
- カード別の採用率と勝率寄与
- **一度も使われなかったカードの一覧**

数値が基準を外れた場合は、報告のみ行い、独断でカードデータを書き換えないこと。

---

## 6. UI規約

- フォント: DotGothic16
- 配色: 森緑（`#2d5a3d`）/ クリーム（`#f5f0e1`）を基調
- タッチ操作前提。タップ領域は最低 44px 四方
- ホバー依存のUIを作らない
- `viewport` 固定、横スクロールを発生させない
- セーブは `localStorage`、キーは `<game-name>_v1` 形式。スキーマ変更時はバージョンを上げ、旧データのマイグレーションを書く

---

## 7. 作業の進め方

1. **SPEC.md を先に書く** — 勝利条件・状態の型定義・画面遷移を確定させ、承認を得てから実装に入る
2. **バーティカルスライス優先** — 全機能を薄く作らず、最小構成を完成品質で1本通す
3. **1コミット1機能** — コミットメッセージは日本語で、変更理由を含める
4. **既存コードは全面書き換えせず、対象箇所のみを編集する**
5. カードやパラメータの大量追加は、ロジックとテストが安定してから最後に行う

---

## 8. 報告のルール

- 作業終了時は「変更したファイル」「テスト結果」「判断に迷った点」の3項目を簡潔に報告する
- 仕様が曖昧な箇所は、推測で実装せず質問する
- 未完了のものを完了として報告しない

---

## 9. 今後の宿題

セッションをまたいで失われないよう、着手前提の課題をここに記録する。

- **core 層の禁止API検査を型レベルに移行する** — 最初のゲームの実装が完了した時点で、core 専用 tsconfig を作成し `lib` から DOM を外す方式に切り替える。現在の文字列マッチによる検査（`tests/core-purity.test.ts`）は、文字列リテラル内を誤検出しうる暫定実装であるため。
- **DotGothic16 のセルフホスト化** — 完全オフライン動作が必要になった時点で、Google Fonts CDN から自前ホストに切り替える。現時点では不要。
